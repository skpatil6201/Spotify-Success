// import PropTypes from 'prop-types'; // Importing PropTypes for prop validation

// // Button component that accepts custom styles and content
// const Button = ({ children, primaryColor, background, borderColor }) => {
//     // Define styles for the button based on passed props
//     const buttonStyles = {
//         color: primaryColor, // Text color of the button
//         backgroundColor: background, // Background color of the button
//         border: `1px solid ${borderColor}` // Border style
//     };

//     return (
//         <button style={buttonStyles} className="rounded-full px-2 py-1">
//             {children} {/* Render button content */}
//         </button>
//     );
// };

// // PropTypes validation for the Button component
// Button.propTypes = {
//     children: PropTypes.node.isRequired, // Content inside the button
//     primaryColor: PropTypes.string.isRequired, // Text color
//     background: PropTypes.string.isRequired, // Background color
//     borderColor: PropTypes.string.isRequired // Border color
// };

// export default Button; // Exporting the Button component
